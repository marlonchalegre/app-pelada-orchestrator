services:
  frontend:
    restart: always
    build:
      context: ./web-peladaapp
      dockerfile: Dockerfile
      target: prod
      args:
        - VITE_APP_VERSION=${TAG:-prod}
        - VITE_GOOGLE_ANALYTICS_ID=${VITE_GOOGLE_ANALYTICS_ID}
    image: peladaapp-frontend:${TAG:-prod}
    ports:
      - 80:80
    volumes:
      - ./nginx/frontend.prod.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend

  backend:
    restart: always
    build:
      context: ./api-peladaapp
      dockerfile: Dockerfile
    image: peladaapp-backend:${TAG:-prod}
    environment:
      - DB_NAME=data/peladaapp.db
      - SKIP_MIGRATIONS=false
      - APP_VERSION=${TAG:-prod}
      - PELADA_API_SECURITY_SIGNING_KEY=${PELADA_API_SECURITY_SIGNING_KEY}
      - WAHA_API_KEY=${WAHA_API_KEY}
      - LITESTREAM_ACCESS_KEY_ID=${LITESTREAM_ACCESS_KEY_ID}
      - LITESTREAM_SECRET_ACCESS_KEY=${LITESTREAM_SECRET_ACCESS_KEY}
      - LITESTREAM_BUCKET=${LITESTREAM_BUCKET}
      - LITESTREAM_ENDPOINT=${LITESTREAM_ENDPOINT}
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ./api-peladaapp/litestream.yml:/etc/litestream.yml:ro

  waha:
    image: ${WAHA_IMAGE:-devlikeapro/waha:arm}
    restart: always
    environment:
      - WAHA_API_KEY=${WAHA_API_KEY}
      - WAHA_DASHBOARD_USERNAME=${WAHA_DASHBOARD_USERNAME}
      - WAHA_DASHBOARD_PASSWORD=${WAHA_DASHBOARD_PASSWORD}
      - WHATSAPP_SWAGGER_USERNAME=${WAHA_DASHBOARD_USERNAME}
      - WHATSAPP_SWAGGER_PASSWORD=${WAHA_DASHBOARD_PASSWORD}
      - WAHA_SWAGGER_ENABLED=true
      - WHATSAPP_API_BASE_URL=https://pelada.mcp.dev.br/waha/
      - WAHA_DASHBOARD_ENABLED=false
    volumes:
      - ./waha_data:/app/.waha/sessions